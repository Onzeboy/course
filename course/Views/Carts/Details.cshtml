@model course.Models.Cart

@{
    ViewData["Title"] = "Details";
}

<h1>Подробнее</h1>

<div>
    <h4>Корзина</h4>
    <hr />
    <dl class="row">
        <dt class = "col-sm-2">
            Название
        </dt>
        <dd class = "col-sm-10">
            @Html.DisplayFor(model => model.ProductsHistory.ID)
        </dd>
        <dd class="col-sm-4">
            @if (!string.IsNullOrEmpty(Model.ProductsHistory.image))
            {

                <img src="@Model.ProductsHistory.image" alt="Изображение" class="img-fluid" />
            }
        </dd>
    </dl>
</div>
<div>
    <a asp-action="Index">Вернуться к списку</a>
</div>
